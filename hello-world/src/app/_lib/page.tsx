export default function PrivateRoute() {
  //for private route save the folder starting with underscore(_foldername)
  //underscore=== url encoded: %5F => _
  return (
    <div>you can not view this in the browser.</div>
  )
}
